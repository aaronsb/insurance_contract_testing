[project]
name = "insurance-contract-verification"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0",
    "pytest-xdist",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
